### 1. 节点自身信息 (JSON)
* **Entity Type**: {}
* **properties**:
    {}
&&*
### 2. 相关实体类型和当前节点实体类型三元组，节点实体类型使用"(当前节点)"作注明
* Format: `{"Source Entity Type", "relation", "Target Entity Type}`
    {tuple}
&&*
### 3. 聚合后的画像摘要
* Format: `{"Entity Type": "profile"}`
    {profile}

